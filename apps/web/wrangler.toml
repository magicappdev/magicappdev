name = "app"
compatibility_date = "2026-01-20"
workers_dev = true
# Note: D1 bindings require a Worker script ('main'). 
# Since this is currently a static asset-only worker (SPA), 
# the database binding is not accessible and causes errors if 'main' is missing.
# Your React app should access data via the API worker.
# [[d1_databases]]
# binding = "DB"
# database_name = "magicappdev-db"
# database_id = "456e0f76-f1f0-44b1-8788-b629a22e39d4"

[assets]
directory = "./dist"
# binding = "ASSETS" # Not allowed in assets-only mode (no main script)
not_found_handling = "single-page-application"
